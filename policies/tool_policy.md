# Tool Usage Policy

Read-only tools: use freely and immediately.
Write tools: require explicit approval.
Prefer the single most specific tool.
Batch independent calls; sequence dependent ones.
On failure: capture the exact error, diagnose root cause,
retry once with a fix, then report.
